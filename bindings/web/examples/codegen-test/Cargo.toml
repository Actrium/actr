[package]
name = "codegen-test"
version = "0.2.0"
edition = "2024"

# Explicitly mark that this crate is not a workspace member
[workspace]

[dependencies]
actr-web-protoc-codegen = { path = "../../crates/web-protoc-codegen" }
anyhow = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
