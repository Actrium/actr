syntax = "proto3";

package echo;

// Echo 请求
message EchoRequest {
  string message = 1;
}

// Echo 响应
message EchoResponse {
  string reply = 1;
  uint64 timestamp = 2;
}

// SendEcho 服务
service SendEcho {
  rpc SendEcho(EchoRequest) returns (EchoResponse);
}
