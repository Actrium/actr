name: EchoApp
options:
  bundleIdPrefix: com.actrium
  deploymentTarget:
    iOS: "26.2"
settings:
  base:
    SUPPORTED_PLATFORMS: "iphoneos iphonesimulator"
packages:
  actr-swift:
    url: https://github.com/Actrium/actr-swift-package-sync
    from: 0.3.3
  swift-protobuf:
    url: https://github.com/apple/swift-protobuf.git
    from: 1.32.0
schemes:
  EchoApp:
    build:
      targets:
        EchoApp: all
    run:
      config: Debug
      environmentVariables:
        - variable: "ACTR_ECHOAPP_AUTO_SEND"
          value: "1"


targets:
  EchoApp:
    type: application
    platform: iOS
    sources:
      - path: EchoApp
      - path: actr.toml
        type: file
        buildPhase: resources
      - path: actr.lock.toml
        type: file
        buildPhase: resources
      - path: manifest.toml
        type: file
        buildPhase: resources
    dependencies:
      - package: actr-swift
        product: Actr
      - package: swift-protobuf
        product: SwiftProtobuf
    info:
      path: EchoApp/Info.plist
      properties:
        CFBundleDisplayName: EchoApp
        UILaunchScreen: {}
        NSLocalNetworkUsageDescription: EchoApp connects to the local actrix development server.
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.actrium.EchoApp
        SWIFT_VERSION: "6.0"
        TARGETED_DEVICE_FAMILY: "1,2"
        SUPPORTED_PLATFORMS: "iphoneos iphonesimulator"
        CODE_SIGN_STYLE: Automatic
