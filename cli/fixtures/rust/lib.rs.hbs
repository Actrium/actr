//! {{PROJECT_NAME}} — Actor-RTC client implementation
//!
//! Generated by `actr init`. Run `actr install` then `actr gen` to populate
//! the `src/generated/` directory with protobuf and actor framework code.

pub mod generated;
{{#if IS_SERVICE}}
pub mod echo_service;
{{/if}}
