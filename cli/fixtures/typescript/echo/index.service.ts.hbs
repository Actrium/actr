async function main(): Promise<void> {
  throw new Error(
    [
      'TypeScript source-defined service workloads were removed.',
      'Build a verified .actr package and host it with Rust Hyper.attach(...).',
    ].join(' '),
  );
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
