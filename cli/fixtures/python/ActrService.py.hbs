# DO NOT EDIT - Generated scaffold

from __future__ import annotations

import argparse
import asyncio
import logging

logging.basicConfig(
    level=logging.INFO,
    format="[%(levelname)s] %(message)s",
)
logger = logging.getLogger(__name__)


async def main() -> int:
    ap = argparse.ArgumentParser(description="{{SERVICE_NAME}} Runner")
    ap.add_argument("--actr-toml", required=True, help="ACTR 配置文件路径")
    ap.parse_args()

    logger.error(
        "{{SERVICE_NAME}} Python source-defined workloads were removed. "
        "Build a verified .actr package and host it with Rust Hyper.attach(...)."
    )
    return 1


if __name__ == "__main__":
    raise SystemExit(asyncio.run(main()))
