edition = 1
exports = ["../proto/data_stream_peer.proto"]

[package]
name = "data-stream-peer-concurrent-client"
description = "Data stream client for peer-to-peer bidirectional streaming with concurrent support"
authors = ["Actr Dev Team"]
license = "Apache-2.0"
tags = ["dev", "example"]

[package.actr_type]
manufacturer = "acme"
name = "DataStreamPeerConcurrentClient"

[dependencies]

[system.signaling]
url = "ws://localhost:8081/signaling/ws"

[system.deployment]
realm_id = 0

[system.discovery]
visible = true

[acl]

[[acl.rules]]
permission = "allow"
types = ["acme+DataStreamPeerConcurrentServer"]

[scripts]
dev = "cargo run"
test = "cargo test"
