edition = 1
exports = ["../proto/echo.proto"]

[package]
name = "wasm-echo-server"
description = "WASM Echo Server - echo service running inside WASM sandbox"
authors = ["Actr Dev Team"]
license = "Apache-2.0"
tags = ["dev", "example", "wasm"]

[package.actr_type]
manufacturer = "acme"
name = "WasmEchoService"
version = "0.1.0"

[dependencies]

[system.signaling]
url = "ws://localhost:8081/signaling/ws"

[system.deployment]
realm_id = 1001

[system.discovery]
visible = true

[system.observability]
filter_level = "info"
tracing_enabled = false
tracing_endpoint = "http://localhost:4317"
tracing_service_name = "wasm-echo-server"

[system.webrtc]
force_relay = false
stun_urls = ["stun:localhost:3478"]
turn_urls = ["turn:localhost:3478"]

# ACL configuration - allow echo client
[acl]

[[acl.rules]]
permission = "allow"
type = "acme:wasm-echo-client-app:0.1.0"
