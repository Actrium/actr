syntax = "proto3";

package echo;

// Echo request
message EchoRequest {
  string message = 1;
}

// Echo response
message EchoResponse {
  string reply = 1;
  uint64 timestamp = 2;
}

// Echo service
service EchoService {
  rpc Echo(EchoRequest) returns (EchoResponse);
}
