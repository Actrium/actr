edition = 1
exports = []

[package]
name = "file-transfer-sender"
description = "File transfer sender using DataStream API"
authors = ["Actr Dev Team"]
license = "Apache-2.0"
tags = ["dev", "example"]

[package.actr_type]
manufacturer = "acme"
name = "GenericClient"
version = "1.0.0"

[dependencies]
file-transfer-receiver = { actr_type = "acme:FileTransferService:1.0.0" }

[system.signaling]
url = "ws://localhost:8081/signaling/ws"

[system.ais_endpoint]
url = "http://localhost:8081/ais"

[system.deployment]
realm_id = 33554432
realm_secret = "rs_AeEwCl5kEEd4foT3KSH-ZSgIFfrBzvnO"

[system.discovery]
visible = true

[system.webrtc]
force_relay = false
stun_urls = ["stun:localhost:3478"]
turn_urls = ["turn:localhost:3478"]

# ACL configuration - allow file transfer receiver
[acl]

[[acl.rules]]
permission = "allow"
# Allow server with actr_type = "acme:FileTransferService:1.0.0"
type = "acme:FileTransferService:1.0.0"

[scripts]
dev = "cargo run"
test = "cargo test"
