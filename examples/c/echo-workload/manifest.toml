name = "c-echo-workload"
manufacturer = "acme"
version = "0.1.0"
description = "C echo workload — authored in C via wit-bindgen c, compiled to a wasm32-wasip2 Component."
authors = ["Actr Dev Team"]
license = "Apache-2.0"
tags = ["dev", "example", "c-guest", "component-model"]
exports = []

[dependencies]
